# 2026-06-29

## 外部 CSS/JS 资源本地化

将项目中所有 HTML 文件的外部 CDN 链接替换为本地路径。

### 完成的工作

1. **下载并本地化 CSS 文件**（3个）：
   - Bootstrap CSS → `assets/css/bootstrap.min.css`
   - Monaco Editor CSS → `assets/css/vs/editor/editor.main.min.css`
   - FullCalendar CSS → `assets/css/fullcalendar.min.css`

2. **下载并本地化 JS 文件**（8个）：
   - jQuery → `assets/js/jquery.min.js`
   - jQuery Throttle/Debounce → `assets/js/jquery.ba-throttle-debounce.min.js`
   - Bootstrap JS → `assets/js/bootstrap.min.js`
   - Monaco Editor loader → `assets/js/vs/loader.min.js`
   - jsPlumb → `assets/js/jsplumb.min.js`
   - Moment.js → `assets/js/moment.min.js`
   - FullCalendar JS → `assets/js/fullcalendar.min.js`
   - FullCalendar gcal → `assets/js/gcal.js`
   - Monaco Editor worker → `assets/js/vs/base/worker/workerMain.min.js`

3. **下载并本地化 Google Fonts**（6个字体文件）：
   - Inconsolata-400.ttf
   - Roboto-300.ttf, Roboto-400.ttf, Roboto-500.ttf
   - WorkSans-400.ttf, WorkSans-700.ttf
   - Google Fonts CSS → `assets/css/google-fonts.css`

4. **更新所有 HTML 文件中的引用**（37个文件）：
   - 使用 Python 脚本批量替换 CSS/JS/字体链接
   - 修复 Monaco Editor 配置中的 CDN 链接（`require.config` 和 `baseUrl`）
   - 修复 `disc/sol-disc01/index.html` 中的 CSS 链接错误（双 `editor`）

### 工具使用
- 使用 curl 下载外部资源
- 编写 Python 脚本批量替换 HTML 文件中的链接
- 使用 Grep 验证替换结果

### 结果
✅ 所有外部 CDN 链接（cdnjs.cloudflare.com、fonts.googleapis.com、fonts.gstatic.com）已替换为本地路径
✅ 所有 37 个 HTML 文件已更新
✅ 临时 Python 脚本已清理

### 注意事项
- 指向外部服务的链接（如 Ed、Gradescope、Python Tutor 等）保持原样，因为它们指向实际的服务
- 如需完全离线使用代码编辑器功能，可能需要下载完整的 Monaco Editor 包（当前只下载了必要的文件）
